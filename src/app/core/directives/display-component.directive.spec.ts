import { DisplayComponentDirective } from './display-component.directive';

describe('DisplayComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayComponentDirective();
    expect(directive).toBeTruthy();
  });
});
